<?xml version="1.0" encoding="utf-8" ?>
<views:BaseContentPage
             xmlns:views="clr-namespace:IDEX.Views"
             xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="IDEX.Views.OverviewPage"
             Title="Site"
             xmlns:Custom="clr-namespace:CustomController;assembly=CustomController">
    <ContentPage.ToolbarItems>
        <ToolbarItem Name="ShowAll" Order="Primary" Text="ShowAll" Priority="0" />
        <ToolbarItem Name="ScanBarcode" Order="Primary" Text="Scan Barcode"  Priority="1" />
    </ContentPage.ToolbarItems>
    
    <StackLayout Orientation="Horizontal">
        <ListView ItemsSource="{Binding ItemListSource}" VerticalOptions="FillAndExpand" HorizontalOptions="StartAndExpand">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <StackLayout Orientation="Horizontal" >
                        <Custom:ExtLabel Style="{StaticResource FirstScreenLabels}" 
                                             HorizontalOptions="StartAndExpand" Text="{Binding Name}"/>
                        <Custom:ShapeView ViewShapeType="CircleIndicator" CornerRadius="2" IndicatorPercentage=".5"/>
                    </StackLayout>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

    </StackLayout>
</views:BaseContentPage>