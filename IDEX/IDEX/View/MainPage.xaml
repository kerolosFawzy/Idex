<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:IDEX"
             x:Class="IDEX.MainPage">

    
    <StackLayout
        Orientation="Vertical" HorizontalOptions="Center" Margin="16">
        <StackLayout Orientation="Horizontal" Spacing="10" >
            <Label Style="{StaticResource FirstScreenLabels}" HorizontalOptions="Start">Customer(s)</Label>
            <Label Style="{StaticResource FirstScreenLabels}" HorizontalOptions="Center">Scheme(s)</Label>
            <Label Style="{StaticResource FirstScreenLabels}" HorizontalOptions="End">Inspection(s)</Label>
        </StackLayout>
        <Image Resources="first_screen_image1.png" Aspect="AspectFit"
               HeightRequest="60" HorizontalOptions="FillAndExpand" Margin="30"
               BackgroundColor="Black"/>
        <StackLayout Orientation="Horizontal" Spacing="20" >
            <ListView HorizontalOptions="Start" BackgroundColor="Pink" >
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <TextCell StyleId="{StaticResource FirstScreenLabels}" Text="{Binding }"/>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
            <ListView HorizontalOptions="Center" BackgroundColor="Gold">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <TextCell StyleId="{StaticResource FirstScreenLabels}" Text="KeroSchema"/>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
            <ListView HorizontalOptions="End" BackgroundColor="Green">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <TextCell StyleId="{StaticResource FirstScreenLabels}" Text="KeroInspection"/>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </StackLayout>
        <ListView BackgroundColor="LightYellow" >
            <ListView.ItemTemplate>
                <DataTemplate>
                    <ImageCell Text="Kero" ImageSource="first_screen_image1.png" TextColor="Black"/>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
        <StackLayout HorizontalOptions="End" Orientation="Horizontal" >
            <Button Text="Back"/>
            <Button Text="next"/>
        </StackLayout>
    </StackLayout>

</ContentPage>
